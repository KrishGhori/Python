a= input("enter the nuber : ")
print("multiplicatony table of {a} is : ")

try:
  for i in  range ( 1, 11 ):
   print(f"{int(a)}X{i}= {int(a)*i}")
except Exception as e :
  print("sorry here is error occurred")

finally :
  print("i am ready")

from collections import deque

def bfs(graph, start):
    visited = set()
    queue = deque([start])

    while queue:
        node = queue.popleft()
        if node not in visited:
            print(node, end=" ")
            visited.add(node)
            queue.extend(neighbor for neighbor in graph.get(node, []) if neighbor not in visited)

# Directed graph adjacency list
graph = {
    'A': ['B', 'C'],
    'B': ['D'],
    'C': ['E'],
    'D': [],
    'E': ['F'],
    'F': []
}

print("BFS Traversal Starting from Node A:")
bfs(graph, 'A')
